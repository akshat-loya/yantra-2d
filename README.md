# yantra-2D

yantra-2D is a custom 2D engine and editor written in C++.

This is an **early skeleton**, designed to set up the repository, CMake build, and folder structure for future development.

## Build Instructions

```bash
mkdir build
cd build
cmake ..
cmake --build .
./bin/Editor
